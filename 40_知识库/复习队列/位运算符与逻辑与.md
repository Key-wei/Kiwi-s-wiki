---
type: review-card
created: 2026-08-09
stage: 0
next_review: 2026-08-10
---
# 位运算符 & 与 && 的区别

概念：& 是按位与（对两个整数的每一位做 AND），&& 是逻辑与（对两个布尔值短路求值）
为什么重要：UE 的位掩码/标志位（如 ECollisionChannel、输入修饰键）大量用按位运算，混用会出隐蔽 bug
能用在哪：判断 flag 是否被设置 (Flags & EMyFlag::X) != 0，而流程判断用 &&
